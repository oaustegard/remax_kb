# Packer-side deps: building a .kb from a corpus.
# Heavyweight (torch + transformers + adapters) — only needed offline.
numpy>=1.24
remax @ git+https://github.com/oaustegard/remax.git@v0.2.0
# remex codec (optional --codec remex). numpy+scipy only.
remex @ git+https://github.com/oaustegard/remex.git@v0.6.0
torch
transformers
sentence-transformers
peft
safetensors
huggingface_hub
